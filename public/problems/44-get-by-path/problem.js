/* jshint esversion: 6 */

// Solve the following prompt using recursion.

// 44. Given an object and an array of keys, return the value at that path.
// Return undefined if the path cannot be followed.
// getByPath({ user: { profile: { name: 'Ada' } } }, ['user', 'profile', 'name']); // 'Ada'
var getByPath = function (object, path) {};