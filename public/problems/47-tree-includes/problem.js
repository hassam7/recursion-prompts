/* jshint esversion: 6 */

// Solve the following prompt using recursion.

// 47. Given a tree where each node has a value and children array, return true if target is found.
// treeIncludes({value: 'root', children: [{value: 'leaf', children: []}]}, 'leaf'); // true
var treeIncludes = function (node, target) {};