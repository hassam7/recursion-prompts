/* jshint esversion: 6 */

// Solve the following prompt using recursion.

// 48. Given an array, return an array containing every subset of the input array.
// The order of subsets does not matter.
// subsets([1, 2]); // [[], [1], [2], [1, 2]]
var subsets = function (array) {};